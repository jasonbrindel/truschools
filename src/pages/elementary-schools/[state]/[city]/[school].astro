---
// Redirect old /elementary-schools/[state]/[city]/[school] URLs to /schools/[state]/[city]/[school]
const { state, city, school } = Astro.params;
return Astro.redirect(`/schools/${state}/${city}/${school}`, 301);
---
